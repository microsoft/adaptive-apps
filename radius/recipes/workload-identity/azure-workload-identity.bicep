// azure-workload-identity.bicep
// Radius Recipe: Radius.Resources/workloadIdentities (AKS + Azure)
//
// Provisions a user-assigned managed identity and optionally configures:
// - federated identity credential for AKS OIDC workload identity
// - Kubernetes service account annotation for azure.workload.identity/client-id
// - Event Grid TopicSpaces RBAC assignment(s)

@description('Information about what resource is calling this Recipe. Generated by Radius. For more information visit https://docs.radapp.io/reference/context-schema/')
param context object

@description('Kubernetes service account to bind workload identity to.')
param serviceAccountName string = 'default'

@description('Whether to create/update the Kubernetes service account with workload identity annotation. Azure recipe currently treats this as informational and does not mutate Kubernetes resources.')
param createServiceAccount bool = true

@description('AKS OIDC issuer URL (for example: https://eastus.oic.prod-aks.azure.com/<tenant>/<guid>/).')
param oidcIssuer string = ''

var namespace = string(context.runtime.?kubernetes.?namespace ?? 'default')
var tokenAudience = 'https://eventgrid.azure.net/'

// Diagnostic mode: no ARM resources are declared so we can isolate whether
// bicep-de crashes before/while processing deployment resources with scope.

output result object = {
  resources: []
  values: {
    clientId: ''
    principalId: ''
    tenantId: ''
    serviceAccountNamespace: namespace
    boundServiceAccountName: serviceAccountName
    authMethod: 'OAUTH2-JWT'
    tokenAudience: tokenAudience
  }
}
