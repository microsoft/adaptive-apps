# ent Capability Portfolio Chart

The `ent` portfolio builds on `core`.

## Portfolio chart

- Chart path: `charts/portfolios/ent`
- Chart name: `ent`

## Dependency chain

- `ent` -> `core` -> `min`
