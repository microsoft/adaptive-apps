# core-ai Capability Portfolio Chart

The `core-ai` portfolio adds AI capabilities on top of `core` (which already provides
Keycloak, Istio service mesh, and observability).

## Portfolio chart

- Chart path: `charts/portfolios/core-ai`
- Chart name: `core-ai`

## Dependency chain

- `core-ai` -> `core` -> `min`
