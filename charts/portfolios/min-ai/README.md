# min-ai Capability Portfolio Chart

The `min-ai` portfolio builds on `min` and is the base of AI-enabled portfolios.

## Portfolio chart

- Chart path: `charts/portfolios/min-ai`
- Chart name: `min-ai`

## Dependency chain

- `min-ai` -> `min`
